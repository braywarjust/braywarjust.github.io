.css */
body {
    font-family: Arial, sans-serif; /* Changed to a standard font */
    margin: 0;
    padding: 0;
    background-color: #f4f4f4;
    letter-spacing: -0.02em; /* Adjusted the value to reduce kerning */
}

nav {
    background: white; /* Changed to white background */
    padding: 20px 0; /* Increased padding to make the nav bar thicker */
}

nav ul {
    list-style: none;
    margin: 0;
    padding: 0;
    text-align: center;
}

nav ul li {
    display: inline;
    margin: 0 15px;
}

nav ul li a {
    color: black; /* Changed to black text */
    text-decoration: none;
    font-weight: bold;
    font-size: 0.9em; /* Slightly smaller font size */
}

nav ul li a:hover {
    text-decoration: underline;
}

.fullscreen-image {
    width: 100%;
    height: 100vh; /* Fullscreen height */
    background-color: white; /* Blank white background for now */
}

h1 {
    margin: 0;
}

h2 {
    color: #333;
}

footer {
    text-align: center;
    padding: 10px 0;
    background: #333;
    color: white;
    position: relative;
    bottom: 0;
    width: 100%;
}